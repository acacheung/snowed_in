%h2 Post a new request
= simple_form_for @request do |f|

  = f.input :job_size,
  :collection => ['small', 'medium', 'big'],
  :as => :radio_buttons

  - if current_user.old_or_disabled == false
    = f.label :offer
    = f.collection_radio_buttons :offer, [[10, '$10'], [20, '$20'], [30, '$30'], [50, '$50']], :first, :last

  %br
  = f.submit 'Submit request'
